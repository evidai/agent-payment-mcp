// Twitter / X large card image. Re-uses the OG implementation so the
// preview is identical across surfaces — at 1200x630 Twitter's "summary_large_image"
// renders the same as Slack / LinkedIn / OG embeds.
export { default, runtime, alt, size, contentType } from "./opengraph-image";
